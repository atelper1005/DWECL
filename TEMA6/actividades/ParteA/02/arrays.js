function recogerDatos() {
    var matriz = [];
    var fila = [];
    var numIntro = 0;
    var conteo = 0;
    
    while(true) {
      numIntro = prompt("Introduzca un valor" + "\nFilas actuales: " + conteo + "\n Pulse c para añadir otra fila" + "\n Pulse f para salir")

      
    }
}

function separarArrays() {
    var pares = [];
    var impares = [];

    if(matriz === 0 || matriz === null) {
      
    }
}

function mostrarArrays() {
  alert("Números pares: " + pares + "\nNúmeros impares: " + impares);
}

